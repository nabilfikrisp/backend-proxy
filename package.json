{
  "private": "true",
  "name": "backend-proxy",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:all": "pnpm --filter shared build && pnpm --filter frontend build && pnpm --filter backend build",
    "start:all": "concurrently \"pnpm --filter frontend start\" \"pnpm --filter backend start\"",
    "dev:all": "concurrently \"pnpm --filter frontend dev\" \"pnpm --filter backend dev\""
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "packageManager": "pnpm@10.20.0",
  "workspaces": [
    "frontend",
    "backend",
    "shared"
  ],
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
